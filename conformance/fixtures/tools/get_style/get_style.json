{
  "id": "get_style.resolve",
  "schemaRef": "tools/get_style.schema.json",
  "validates": "inputs",
  "deferred": true,
  "inputs": {
    "styleId": "style_flood_depth",
    "encoding": "mapbox-style",
    "includeStylesheet": true
  },
  "expected": "tool_result",
  "canonicalRefs": ["StyleRef"],
  "notes": "Reference implementation does not yet ship get_style as a discrete tool; resolves a read-only StyleRef projection from honua://styles/{style_id}. See spec/conformance.md §2.4."
}
