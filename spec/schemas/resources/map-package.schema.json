{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://geospatial-mcp.honua.io/spec/schemas/resources/map-package.schema.json",
  "title": "Map package resource payload",
  "description": "Responsibility-level projection of a MapPackage at honua://map-packages/{map_package_id}. Concrete field spellings finalize upstream (honua-server#731); only the stable mapPackageId is pinned and additional properties are permitted. Responsibilities: package format/template binding, source bindings, styling composition, map-spec document, initial view, legend/popup/label composition, artifact bindings. See resources.md §honua://map-packages/{map_package_id}.",
  "type": "object",
  "additionalProperties": true,
  "required": ["mapPackageId"],
  "properties": {
    "mapPackageId": {
      "type": "string",
      "description": "Stable identifier (map_…)."
    },
    "templateId": { "type": "string", "description": "Bound MapTemplate." },
    "sourceBindings": { "type": "array", "items": { "type": "object", "additionalProperties": true } },
    "styleId": { "type": "string" },
    "themeId": { "type": "string" },
    "initialView": { "type": "object", "additionalProperties": true },
    "previewArtifactId": { "type": "string" },
    "controls": {
      "$ref": "../common/controls.schema.json#/properties/controls",
      "description": "Standard-owned input affordances; the collection `control:{id}` references resolve against (ADR-0031)."
    },
    "explorationContexts": {
      "$ref": "../common/exploration.schema.json#/properties/explorationContexts",
      "description": "Standard-owned units of shared interaction state; the primary interaction model, which components join by naming a context (ADR-0032)."
    },
    "interactions": {
      "$ref": "../common/interactions.schema.json#/properties/interactions",
      "description": "Standard-owned declarative event→action bindings, narrowed by ADR-0032 to one-shot gestures (ADR-0030)."
    },
    "layout": {
      "$ref": "../common/interactions.schema.json#/properties/layout",
      "description": "Standard-owned grid placement for widgets (ADR-0030)."
    }
  }
}
