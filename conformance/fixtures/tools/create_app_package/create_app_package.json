{
  "id": "create_app_package.dashboard",
  "schemaRef": "tools/create_app_package.schema.json",
  "validates": "inputs",
  "inputs": {
    "templateId": "analysis_dashboard",
    "targetSdk": "honua-sdk-js",
    "mapPackageId": "map_5a90",
    "boundArtifactIds": ["artifact_summary_report"],
    "runtimeConfig": { "title": "Flood Exposure Dashboard" }
  },
  "expected": "tool_result",
  "canonicalRefs": ["BuilderIntent", "AppPackage", "MapPackage", "ArtifactRef"],
  "notes": "The reference implementation ships create_app_package as a discrete tool (advertised honua_create_app_package; see conformance/manifests/honua.manifest.json) and creates the draft package deterministically from these inputs, so this fixture is not deferred. Responsibility-level input shape per spec/schemas/tools/create_app_package.schema.json."
}
